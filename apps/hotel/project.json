{
    "name": "hotel",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/hotel/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nx/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/hotel",
                "main": "apps/hotel/src/main.ts",
                "tsConfig": "apps/hotel/tsconfig.app.json",
                "assets": ["apps/hotel/src/assets"],
                "isolatedConfig": true,
                "webpackConfig": "apps/hotel/webpack.config.js"
            },
            "configurations": {
                "development": {},
                "production": {}
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "hotel:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "hotel:build:development"
                },
                "production": {
                    "buildTarget": "hotel:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/hotel/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/hotel/jest.config.ts"
            }
        }
    },
    "tags": []
}
