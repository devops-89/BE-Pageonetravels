{
    "name": "utility-services",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/utility-services/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nx/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/utility-services",
                "main": "apps/utility-services/src/main.ts",
                "tsConfig": "apps/utility-services/tsconfig.app.json",
                "assets": ["apps/utility-services/src/assets"],
                "isolatedConfig": true,
                "webpackConfig": "apps/utility-services/webpack.config.cjs"
            },
            "configurations": {
                "development": {},
                "production": {}
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "utility-services:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "utility-services:build:development"
                },
                "production": {
                    "buildTarget": "utility-services:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/utility-services/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/utility-services/jest.config.ts"
            }
        }
    },
    "tags": []
}
