{
    "name": "hotel_management",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/hotel_management/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nx/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/hotel_management",
                "main": "apps/hotel_management/src/main.ts",
                "tsConfig": "apps/hotel_management/tsconfig.app.json",
                "assets": ["apps/hotel_management/src/assets"],
                "isolatedConfig": true,
                "webpackConfig": "apps/hotel_management/webpack.config.js"
            },
            "configurations": {
                "development": {},
                "production": {}
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "hotel_management:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "hotel_management:build:development"
                },
                "production": {
                    "buildTarget": "hotel_management:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/hotel_management/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/hotel_management/jest.config.ts"
            }
        }
    },
    "tags": []
}
