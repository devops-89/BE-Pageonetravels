{
    "name": "admin-panel",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/admin-panel/src",
    "projectType": "application",
    "targets": {
        "build": {
            "executor": "@nx/webpack:webpack",
            "outputs": ["{options.outputPath}"],
            "defaultConfiguration": "production",
            "options": {
                "target": "node",
                "compiler": "tsc",
                "outputPath": "dist/apps/admin-panel",
                "main": "apps/admin-panel/src/main.ts",
                "tsConfig": "apps/admin-panel/tsconfig.app.json",
                "assets": ["apps/admin-panel/src/assets"],
                "isolatedConfig": true,
                "webpackConfig": "apps/admin-panel/webpack.config.js"
            },
            "configurations": {
                "development": {},
                "production": {}
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "defaultConfiguration": "development",
            "options": {
                "buildTarget": "admin-panel:build"
            },
            "configurations": {
                "development": {
                    "buildTarget": "admin-panel:build:development"
                },
                "production": {
                    "buildTarget": "admin-panel:build:production"
                }
            }
        },
        "lint": {
            "executor": "@nx/eslint:lint",
            "outputs": ["{options.outputFile}"],
            "options": {
                "lintFilePatterns": ["apps/admin-panel/**/*.ts"]
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
            "options": {
                "jestConfig": "apps/admin-panel/jest.config.ts"
            }
        }
    },
    "tags": []
}
